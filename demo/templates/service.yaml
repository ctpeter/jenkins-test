apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo.fullname" . }}
  labels:
    {{- include "demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:sssss
    - port: {{ .Vssssalues.service.port }}
      targetPort: ssss
      protocol: TCP
      name: http
  selectorsss
    {{- includesss"demo.selectorLabels" . | nindent 4 }}
